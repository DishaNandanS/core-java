package interfaceHw;
//Create an 3 interface and interface has 2 child class, In Runner class create implementation object.

public interface School {

	public abstract void totalMarks();// for these abstract class can't create object in runner
	public abstract void studentGrade();
	
	}
